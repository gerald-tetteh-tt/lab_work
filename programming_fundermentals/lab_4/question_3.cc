// Programming fundermentals: conditional flow statements and looping constructs
// Question 3

#include <iostream>

int main()
{
    // ^Z is the end of line for windows
    char c;
    while(std::cin.get(c))
        std::cout.put(c);
}